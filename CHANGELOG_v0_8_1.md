# v0.8.1 Single Icon

- PuttVision Deploy launcher icon removed
- PuttVision Screen is the only launcher icon
- ZIP sharing still opens DeployActivity
- Settings / Environment now contains ZIP Deploy button
- Removing/uninstalling PuttVision Screen still removes the whole app package, as expected
